# Environment Configuration for LDK Node with LSPS2 Liquidity

# LSPS2 Liquidity Service Provider (LSP) Configuration
# The public key of the LSPS2 peer you want to use for liquidity
LSP_PUBKEY=

# The IP address and port of the LSPS2 liquidity provider
LSP_ADDRESS=
# Bitcoin Network Configuration
# Options: bitcoin, testnet, regtest, signet
NETWORK=bitcoin

# Blockchain Data Source
# Esplora API endpoint for blockchain data
ESPLORA_API_URL=https://blockstream.info/api

# Optional: Log Level
# Options: Trace, Debug, Info, Warn, Error
LOG_LEVEL=Debug

# Optional: Invoice Expiry (for manual_claim_jit example)
# How long before the invoice expires (in seconds)
# Default: 3600 (1 hour)
# For fast testing: 300 (5 minutes)
INVOICE_EXPIRY_SECS=3600

# Optional: Minimum Final CLTV Expiry Delta (for manual_claim_jit example)
# Controls how many blocks before HTLC timeout (ACTUAL timeout mechanism)
# This is the critical parameter for testing HTLC holds!
# 
# Recommended values by network:
# - regtest: 10-20 blocks (instant block mining, so can be very low)
# - testnet: 18 blocks (~3 hours with 10 min blocks)
# - signet: 18 blocks (~3 hours)
# - mainnet: 80 blocks (~13 hours) - DEFAULT if not specified
#
# ⚠️ WARNING: Lower values increase risk of force-close on mainnet!
# Only use low values on test networks or if you understand the risks.
#
# For FAST testing (regtest): 10
# For FAST testing (testnet/signet): 18  
# For production (mainnet): 80 (or omit this line)
MIN_FINAL_CLTV_EXPIRY_DELTA=80
